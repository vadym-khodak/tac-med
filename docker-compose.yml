services:
  server:
    build:
      context: ./server
      dockerfile: ./Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGO_URI=mongodb://mongo:27017/tac-med
      - ORIGIN_URL=http://192.168.88.87:4200
      - AUTH0_DOMAIN=${AUTH0_DOMAIN}
      - AUTH0_AUDIENCE=${AUTH0_AUDIENCE}
      - SENTRY_DSN=${SENTRY_DSN}
    depends_on:
      - mongo
    networks:
      - tac-med-network

  client:
    build:
      context: ./client
      dockerfile: ./Dockerfile
    ports:
      - "4200:80"
    environment:
      - VITE_API_URL=http://192.168.88.87:3000
    depends_on:
      - server
    networks:
      - tac-med-network

  mongo:
    image: mongo:7
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - tac-med-network

networks:
  tac-med-network:
    driver: bridge

volumes:
  mongo-data:
